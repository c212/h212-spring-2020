Step 5

Next I did the same for a class called Paths. This class was used to 
store groups of paths when implementing BFS. I added both a constructor 
that takes no arguments to create an empty set of paths and one that can 
take a Path and creates a new set of paths with that path added.