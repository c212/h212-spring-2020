Step 10. 

Part 2

Next I added a method drawNode() that draws a circle labeled with 
the node name. I added a for loop in the paintComponent method that 
loops through each node in the graph and draws it in a random place 
within the JFrame.