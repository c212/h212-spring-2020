Step 12.

Step 4

Then, I created two methods: lightNode() and unlightNode() for 
lighting and unlighting the nodes when I want to visualize BFS. 
I tested the lightNode() method in the paintComponent() method.